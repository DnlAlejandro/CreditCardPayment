import { createTheme } from "@mui/material";

export const greenTheme = createTheme({
    palette: {
        primary: {
            main: "#1b4332"
        },
        secondary: {
            main: "#2d6a4f"
        },
        error: {
            main: "#ee6055"
        }
    },
});
